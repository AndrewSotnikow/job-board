export const selectAllFilter = (state) => state.filter
export const selectRemoveFilter = (state) => state.filter
export const selectClearFilter = (state) => state.filter
export const selectFilters = (state) => state.filter
